version: '2'
services:

  # The Application
  app:
    build:
      context: ./
      dockerfile: ./docker/app.dockerfile
    working_dir: /var/www
    volumes:
      - ./:/var/www
    command: "php artisan serve --host=0.0.0.0"
    ports:
      - 8000:8000
